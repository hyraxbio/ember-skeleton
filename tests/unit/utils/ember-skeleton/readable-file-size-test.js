import emberSkeletonReadableFileSize from 'dummy/utils/ember-skeleton/readable-file-size';
import { module, test } from 'qunit';

module('Unit | Utility | ember-skeleton/readable-file-size', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = emberSkeletonReadableFileSize();
    assert.ok(result);
  });
});
