import customInflector from 'dummy/utils/custom-inflector';
import { module, test } from 'qunit';

module('Unit | Utility | custom-inflector', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = customInflector();
    assert.ok(result);
  });
});
