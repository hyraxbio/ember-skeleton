import humaniseString from 'dummy/utils/humanise-string';
import { module, test } from 'qunit';

module('Unit | Utility | humanise-string', function (hooks) {
  // Replace this with your real tests.
  test('it works', function (assert) {
    let result = humaniseString();
    assert.ok(result);
  });
});
