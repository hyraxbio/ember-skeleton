import contrastColor from 'dummy/utils/contrast-color';
import { module, test } from 'qunit';

module('Unit | Utility | contrast-color', function () {
  // Replace this with your real tests.
  test('it works', function (assert) {
    let result = contrastColor();
    assert.ok(result);
  });
});
