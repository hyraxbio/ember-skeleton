import themeColorString from 'dummy/utils/theme-color-string';
import { module, test } from 'qunit';

module('Unit | Utility | theme-color-string', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = themeColorString();
    assert.ok(result);
  });
});
