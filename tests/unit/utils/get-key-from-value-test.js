import getKeyFromValue from 'dummy/utils/get-key-from-value';
import { module, test } from 'qunit';

module('Unit | Utility | getKeyFromValue', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = getKeyFromValue();
    assert.ok(result);
  });
});
