import robotiseString from 'dummy/utils/robotise-string';
import { module, test } from 'qunit';

module('Unit | Utility | robotise-string', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = robotiseString();
    assert.ok(result);
  });
});
